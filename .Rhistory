models = list(
lm = lm_spec,
rf = rf_spec,
glm = glm_spec,
xgb = xgb_spec
),
cross = TRUE  # Create all combinations
)
# View the workflow set
print(model_set)
#doParallel::registerDoParallel()
model_results <- model_set |>
workflow_map(
fn = "tune_grid",
resamples = rb_folds,
grid = 10,  # Number of hyperparameter combinations to try
metrics = metric_set(rmse, rsq, mae),
verbose = TRUE,
seed = 123
)
# View results summary
# Collect and rank metrics
model_rankings <- model_results |>
rank_results(rank_metric = "rmse", select_best = TRUE) |>
filter(.metric == "rmse")
print(model_rankings)
# Visualize model performance
autoplot(model_results, metric = "rmse") +
theme_minimal() +
labs(title = "Model Performance Comparison",
subtitle = "Lower RMSE indicates better performance") +
theme(axis.text.x = element_text(angle = 45, hjust = 1))
best_workflow_id <- model_rankings |>
slice_head(n = 1) |>
pull(wflow_id)
model_rankings
best_workflow <- model_results |>
extract_workflow(best_workflow_id)
# Finalize the best model with optimal hyperparameters
best_results <- model_results |>
extract_workflow_set_result(best_workflow_id)
best_params <- best_results |>
select_best(metric = "rmse")
final_workflow <- best_workflow |>
finalize_workflow(best_params)
# Fit the final model on training data and evaluate on test data
final_fit <- final_workflow |>
last_fit(splits)
final_workflow
# Test set performance
test_performance <- final_fit |>
collect_metrics()
print("Test Set Performance:")
print(test_performance)
# Predictions vs actual plot
final_fit |>
collect_predictions() |>
ggplot(aes(x = touches, y = .pred)) +
geom_point(alpha = 0.7) +
geom_abline(slope = 1, intercept = 0, color = "red", linetype = "dashed") +
labs(x = "Actual Touches", y = "Predicted Touches",
title = "Best Model: Predictions vs Actual") +
theme_minimal()
# Create a detailed comparison plot
model_results |>
collect_metrics() |>
filter(.metric == "rmse") |>
ggplot(aes(x = reorder(wflow_id, mean), y = mean)) +
geom_point(size = 2) +
geom_errorbar(aes(ymin = mean - std_err, ymax = mean + std_err),
width = 0.1) +
coord_flip() +
labs(x = "Workflow", y = "RMSE",
title = "Model Performance Comparison with Error Bars") +
theme_minimal()
rb_train |>
filter(years_exp == 0) -> rookie_train
rb_train |>
filter(years_exp == 0)  -> rookie_test
rookie_train |>
ggplot(aes(x = draft_number, y= touches)) +
geom_point() +
geom_smooth(method = "gam")
library(mgcv)
# Fit our baseline model - simple linear regression
rookie_fit <- lm(touches ~ season + draft_number + height*weight, data = rookie_train)
# Fit a GAM with 1 predictor x and response y - This was tinkered with
gam_model <- gam(touches ~ s(draft_number) + s(weight) + height + season, data = rookie_train)
# Some more tinkering just cause
gam_model2<- gam(touches ~ s(draft_number) + te(weight, height) + weight,data = rookie_train)
broom::glance(rookie_fit)
broom::glance(gam_model)
summary(gam_model)
summary(gam_model2)
tidy(gam_model, conf.int = TRUE)
# Make predictions on test set
augment(gam_model,rookie_test) |>
select(player_id,player_name,recent_team, season,touches,.fitted,.resid,draft_number,height,weight) -> res_rookie
res_rookie |>
arrange(desc(season),desc(.resid))
# SSE
sqrt(mean((res_rookie$.fitted-res_rookie$touches)**2))
rmse(res_rookie, touches,.fitted)
res_rookie |>
ggplot(aes(x = draft_number, y = abs(.resid))) +
geom_point()+
geom_smooth(method = "lm")
model_data |>
group_by(age = round(approx_age)) |>
summarise(avg_carries = mean(carries,na.rm = T)) |>
ungroup() |>
ggplot(aes(x = age,y=avg_carries))+
geom_smooth()
model_data$age
model_data |>
group_by(age = age) |>
#summarise(avg_carries = mean(carries,na.rm = T)) |>
ungroup() |>
ggplot(aes(x = age,y=touches))+
geom_smooth()
model_data |>
group_by(age = age) |>
#summarise(avg_carries = mean(carries,na.rm = T)) |>
ungroup() |>
ggplot(aes(x = age,y=touches))+
geom_point()
model_data |>
group_by(player_id,player_name) |>
summarise(last_age = max(age))
# Measure the changes year-to-year of w/ RBs having at least 50 touches
model_data |>
filter(touches >= 50) |>
select(player_id,player_name,age,touches) |>
left_join(select(model_data,player_id,player_name,age,touches),by = c(player_id,age = age-1))
model_data |>
filter(touches >= 50) |>
select(player_id,player_name,age,touches)
# Measure the changes year-to-year of w/ RBs having at least 50 touches
model_data |>
filter(touches >= 50) |>
select(player_id,player_name,age,touches) |>
left_join(select(model_data,player_id,player_name,age,touches))
# Measure the changes year-to-year of w/ RBs having at least 50 touches
model_data |>
filter(touches >= 50) |>
select(player_id,player_name,age,touches) |>
left_join(select(model_data,player_id,player_name,age,touches), by = c(player_id,age = age-1))
model_data |>
filter(touches >= 50) |>
select(player_id,player_name,age,touches) |>
mutate(next_age = age -1)
model_data |>
filter(touches >= 50) |>
select(player_id,player_name,age,touches) |>
mutate(next_age = age -1) -> minus1
minus1 |>
inner_join(minus1,by = c("player_id","age" = "next_age"))
minus1 |>
inner_join(minus1,by = c("player_id","age" = "next_age"))
mutate(diff_touches = touches.x - touches.y)
minus1 |>
inner_join(minus1,by = c("player_id","age" = "next_age")) |>
mutate(diff.touches = touches.x - touches.y)
minus1 |>
inner_join(minus1,by = c("player_id","age" = "next_age")) |>
mutate(diff.touches = touches.x - touches.y)
minus1 |>
inner_join(minus1,by = c("player_id","age" = "next_age")) |>
mutate(diff.touches = touches.x - touches.y)
model_data |>
filter(touches >= 50) |>
select(player_id,age,touches) |>
mutate(next_age = age -1) -> minus1
minus1 |>
inner_join(minus1,by = c("player_id","age" = "next_age")) |>
mutate(diff.touches = touches.x - touches.y)
minus1 |>
inner_join(minus1,by = c("player_id","age" = "next_age")) |>
mutate(diff.touches = touches.x - touches.y) |>
select(player_id,age,next_age,diff.touches)
model_data |>
filter(touches >= 50) |>
select(player_id,age,touches) |>
mutate(next_age = age +1) -> minus1
minus1 |>
inner_join(minus1,by = c("player_id","age" = "next_age")) |>
mutate(diff.touches = touches.x - touches.y) |>
select(player_id,age,next_age,diff.touches) |>
ggplot(aes(x = ))
minus1 |>
inner_join(minus1,by = c("player_id","age" = "next_age")) |>
mutate(diff.touches = touches.x - touches.y) |>
select(player_id,age,next_age,diff.touches)
minus1 |>
inner_join(minus1,by = c("player_id","age" = "next_age")) |>
mutate(diff.touches = touches.x - touches.y) |>
select(player_id,age,next_age,diff.touches)
minus1 |>
inner_join(minus1,by = c("player_id","age" = "next_age")) |>
mutate(diff.touches = touches.x - touches.y) |>
select(player_id,age,next_age,diff.touches) -> age_diffs
age_diffs |>
group_by(age,next_age) |>
summarise(avg_diff = mean(diff.touches))
age_diffs
minus1 |>
inner_join(minus1,by = c("player_id","age" = "next_age")) |>
mutate(diff.touches = touches.y - touches.x) |>
select(player_id,age,next_age,diff.touches) -> age_diffs
age_diffs
age_diffs |>
group_by(age,next_age) |>
summarise(avg_diff = mean(diff.touches))
minus1 |>
inner_join(minus1,by = c("player_id","age" = "next_age")) |>
mutate(diff.touches = touches.y - touches.x)
minus1 |>
inner_join(minus1,by = c("player_id","age" = "next_age"))
model_data |>
filter(touches >= 50) |>
select(player_id,age,touches) |>
mutate(next_age = age +1) -> minus1
minus1
minus1 |>
inner_join(minus1,by = c("player_id", "next_age" = "age")) |>
mutate(diff.touches = touches.y - touches.x)
minus1 |>
inner_join(minus1,by = c("player_id", "next_age" = "age")) |>
mutate(diff.touches = touches.y - touches.x) |>
select(player_id,age,next_age,diff.touches) -> age_diffs
age_diffs
age_diffs |>
group_by(age,next_age) |>
summarise(avg_diff = mean(diff.touches))
minus1 |>
inner_join(minus1,by = c("player_id", "next_age" = "age")) |>
mutate(diff.touches = touches.y - touches.x) |>
select(player_id,age,next_age,diff.touches) -> age_diffs
View(age_diffs)
minus1 |>
inner_join(minus1,by = c("player_id", "next_age" = "age"),suffix = c(".current",".next")) |>
mutate(diff.touches = touches.current - touches.next)
minus1 |>
inner_join(minus1,by = c("player_id", "next_age" = "age"),suffix = c(".current",".next")) |>
mutate(diff.touches = touches.next - touches.current) |>
select(player_id,age,next_age,diff.touches) -> age_diffs
age_diffs
age_diffs |>
group_by(age,next_age) |>
summarise(avg_diff = mean(diff.touches))
age_diffs |>
group_by(age,next_age) |>
summarise(avg_diff = mean(diff.touches)) |>
ggplot(aes(x = age,y = next_age)) |>
geom_point() +
geom_line() +
labs(title = "Delta Method", x = "Age", y = "Avg. Change in Total Touches")
age_diffs |>
group_by(age,next_age) |>
summarise(avg_diff = mean(diff.touches)) |>
ggplot(aes(x = age,y = next_age)) |+
geom_point() +
geom_line() +
labs(title = "Delta Method", x = "Age", y = "Avg. Change in Total Touches")
age_diffs |>
group_by(age,next_age) |>
summarise(avg_diff = mean(diff.touches)) |>
ggplot(aes(x = age,y = next_age)) +
geom_point() +
geom_line() +
labs(title = "Delta Method", x = "Age", y = "Avg. Change in Total Touches")
age_diffs |>
group_by(age,next_age) |>
summarise(avg_diff = mean(diff.touches))
age_diffs |>
group_by(age,next_age) |>
summarise(avg_diff = mean(diff.touches)) |>
ggplot(aes(x = age,y = next_age)) +
geom_point()
age_diffs |>
group_by(age,next_age) |>
summarise(avg_diff = mean(diff.touches)) |>
ggplot(aes(x = next_age,y = diff.touches)) +
geom_point() +
geom_line() +
labs(title = "Delta Method", x = "Age", y = "Avg. Change in Total Touches")
age_diffs |>
group_by(age,next_age) |>
summarise(avg_diff = mean(diff.touches)) |>
ggplot(aes(x = next_age,y = avg_diff)) +
geom_point() +
geom_line() +
labs(title = "Delta Method", x = "Age", y = "Avg. Change in Total Touches")
age_diffs |>
group_by(age,next_age) |>
summarise(avg_diff = mean(diff.touches),n())
age_diffs |>
group_by(age,next_age) |>
summarise(avg_diff = mean(diff.touches),n = n()) |>
ungroup() |>
# Arbitrary cutoff (20 players)
filter(n >= 20) |>
ggplot(aes(x = next_age,y = avg_diff)) +
geom_point() +
geom_line() +
labs(title = "Delta Method", x = "Age", y = "Avg. Change in Total Touches")
age_diffs |>
group_by(age,next_age) |>
summarise(avg_diff = mean(diff.touches),n = n()) |>
ungroup() |>
# Arbitrary cutoff (20 players)
filter(n >= 20) |>
ggplot(aes(x = next_age,y = avg_diff)) +
geom_point() +
geom_smooth() +
labs(title = "Delta Method", x = "Age", y = "Avg. Change in Total Touches")
age_diffs |>
group_by(age,next_age) |>
summarise(avg_diff = mean(diff.touches),n = n()) |>
ungroup() |>
# Arbitrary cutoff (20 players)
filter(n >= 20) |>
ggplot(aes(x = next_age,y = avg_diff)) +
geom_point() +
geom_smooth(se = F) +
labs(title = "Delta Method", x = "Age", y = "Avg. Change in Total Touches")
rb_roster_info |>
filter(gsis_id == "00-0032780")
# Visualize model performance
autoplot(model_results, metric = "rmse") +
theme_minimal() +
labs(title = "Model Performance Comparison") +
theme(axis.text.x = element_text(angle = 45, hjust = 1))
# Visualize model performance
autoplot(model_results, metric = "rmse") +
theme_minimal() +
labs(title = "Model Performance Comparison") +
theme(axis.text.x = element_text(angle = 45, hjust = 1))
best_workflow_id <- model_rankings |>
slice_head(n = 1) |>
pull(wflow_id)
model_rankings
max(model_data$season)
agg_stats |>
left_join(y = roster_contract,by = c("season","player_id" = "gsis_id")) |>
filter(season >= 2009) |>
# Create a new target: "Total Touches"
mutate(touches = carries + receptions) |>
arrange(desc(season)) |>
# Select relevant columns for modeling
select(1:4,ends_with("_sum"),ends_with("_lag1"),"touches",36:44,-c(apy)) -> full_data
# We want to hold out the most recent season's data to visualize
full_data |>
filter(season <= max(full_data$season))
# We want to hold out the most recent season's data to visualize
full_data |>
filter(season << max(full_data$season))
# We want to hold out the most recent season's data to visualize
full_data |>
filter(season < max(full_data$season))
# We want to hold out the most recent season's data to visualize
full_data |>
filter(season < max(full_data$season)) -> model_data
library(tidymodels)
set.seed(123)
# Split the data
splits <- rsample::initial_split(model_data, prop = 0.8)
rb_train <- rsample::training(splits)
rb_test <- rsample::testing(splits)
# Create cross-validation folds
rb_folds <- vfold_cv(rb_train, v = 5)
some_vars <- names(model_data)[5:ncol(model_data)]
# Define preprocessing recipes
# Recipe 1: Basic preprocessing
basic_recipe <- recipe(touches ~ ., data = rb_train) |>
update_role(season, new_role = "ID") |>
update_role(player_id, new_role = "ID") |>
step_select(all_of(some_vars)) |>
step_normalize(all_numeric_predictors()) |>
step_corr(all_numeric_predictors(), threshold = 0.9) |>
step_impute_median(apy_cap_pct) |>
step_naomit(all_numeric_predictors()) |>
step_filter_missing(
threshold = 0.3
)
pca_recipe <- basic_recipe |>
step_pca(all_numeric_predictors(), threshold = 0.95)
# Define model specifications
# Linear regression
lm_spec <- linear_reg() |>
set_engine("lm")
# Random Forest
rf_spec <- rand_forest(
mtry = tune(),
trees = 1000,
min_n = tune()
) |>
set_engine("ranger") |>
set_mode("regression")
# Elastic Net
glm_spec <- linear_reg(
penalty = tune(),
mixture = tune()
) |>
set_engine("glmnet")
# XGBoost
xgb_spec <- boost_tree(
mtry = tune(), trees = 1000, min_n = tune(), tree_depth = tune(),
learn_rate = tune(), loss_reduction = tune(), sample_size = NULL,
stop_iter = tune()
) |>
set_engine("xgboost") |>
set_mode("regression")
prep(basic_recipe)
# Create workflow set
# Combine different recipes with different models
model_set <- workflow_set(
preproc = list(
basic = basic_recipe,
pca = pca_recipe
),
models = list(
lm = lm_spec,
rf = rf_spec,
glm = glm_spec,
xgb = xgb_spec
),
cross = TRUE  # Create all combinations
)
# View the workflow set
print(model_set)
#doParallel::registerDoParallel()
model_results <- model_set |>
workflow_map(
fn = "tune_grid",
resamples = rb_folds,
grid = 10,  # Number of hyperparameter combinations to try
metrics = metric_set(rmse, rsq, mae),
verbose = TRUE,
seed = 123
)
# View results summary
# Collect and rank metrics
model_rankings <- model_results |>
rank_results(rank_metric = "rmse", select_best = TRUE) |>
filter(.metric == "rmse")
print(model_rankings)
best_workflow <- model_results |>
extract_workflow(best_workflow_id)
# Finalize the best model with optimal hyperparameters
best_results <- model_results |>
extract_workflow_set_result(best_workflow_id)
best_params <- best_results |>
select_best(metric = "rmse")
final_workflow <- best_workflow |>
finalize_workflow(best_params)
# Fit the final model on training data and evaluate on test data
final_fit <- final_workflow |>
last_fit(splits)
final_workflow
splits
# Fit the final model on training data and evaluate on test data
final_fit <- final_workflow |>
fit(data = rb_train)
preds <- predict(final_fit, new_data = filter(full_data, season == max(full_data$season)))
full_data$touches
sum(is.na(full_data))
df.2024 = filter(full_data, season == max(full_data$season))
preds <- predict(final_fit, new_data = df.2024)
rb_train$touches
df.2024$touches
df.2024 = filter(full_data, season == max(full_data$season)) |> select(-touches)
preds <- predict(final_fit, new_data = df.2024)
final_fit %>% extract_recipe() %>% tidy()
final_fit %>% extract_recipe() %>% tidy()
final_fit %>% extract_recipe() %>% tidy() -> df.recipe
View(df.recipe)
final_fit$fit
model_rankings
model_rankings |>
slice_head(n = 1) |>
pull(wflow_id)
best_workflow_id <- model_rankings |>
slice_head(n = 1) |>
pull(wflow_id)
best_workflow <- model_results |>
extract_workflow(best_workflow_id)
best_workflow
# Finalize the best model with optimal hyperparameters
best_results <- model_results |>
extract_workflow_set_result(best_workflow_id)
best_results
best_params <- best_results |>
select_best(metric = "rmse")
final_workflow <- best_workflow |>
finalize_workflow(best_params)
final_workflow
# Fit the final model on training data and evaluate on test data
final_fit <- final_workflow |>
fit(data = rb_train)
final_fit
df.2024 = filter(full_data, season == max(full_data$season)) |> select(-touches)
preds <- predict(final_fit, new_data = df.2024)
df.2024 = filter(full_data, season == max(full_data$season))
preds <- predict(final_fit, new_data = df.2024)
rlang::last_trace()
bake(object = basic_recipe,new_data = df.2024)
prep(x = basic_recipe)
bake(object = basic_recipe,new_data = df.2024)
names(df.2024)
str(rb_train$touches)
str(df.2024$touches)
